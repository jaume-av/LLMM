# Ciutats


## `routes/ciutats.js`

```javascript
router.put('/:id', function(req, res) {
    db('CIUTAT')
        .where('ID', req.params.id)
        .update({
            NOM: req.body.NOM,
            DESCRIPCIO: req.body.DESCRIPCIO,
            IMATGE: req.body.IMATGE,
            POBLACIO: req.body.POBLACIO,
            PROVINCIA_ID: req.body.PROVINCIA_ID
        })
        .then(function(resultat) {
            res.json({ missatge: 'Ciutat modificada correctament' });
        })
        .catch(function(error) {
            res.status(500).json({ error: 'Error en la base de dades' });
        });
});
```

---

## `public/js/ciutats-vista.js`

### Botó `Modificar`

```html
<button type="button"
        class="success"
        @click="mostrarModificarCiutat(ciutat.ID)">
    Modificar
</button>
```

### Vista de modificació

```javascript
function vistaModificarCiutat(id) {
    return `
        <section x-data="modificarCiutatApp(${id})"
                 x-init="carregarCiutat()">

            <h2>Modificar ciutat</h2>

            <form @submit.prevent="modificarCiutat()">

                <label for="nom">Nom</label>
                <input id="nom" type="text" x-model="ciutat.NOM">

                <label for="descripcio">Descripció</label>
                <input id="descripcio" type="text" x-model="ciutat.DESCRIPCIO">

                <label for="poblacio">Població</label>
                <input id="poblacio" type="number" x-model="ciutat.POBLACIO">

                <label for="provincia">Província</label>
                <input id="provincia" type="number" x-model="ciutat.PROVINCIA_ID">

                <label for="imatge">Imatge</label>
                <input id="imatge" type="text" x-model="ciutat.IMATGE">

                <p>
                    <button type="submit">Guardar canvis</button>

                    <button type="button"
                            class="warning"
                            onclick="mostrarCiutats()">
                        Cancel·lar
                    </button>
                </p>

            </form>

        </section>
    `;
}
```

### App Alpine

```javascript
window.modificarCiutatApp = function (id) {
    return {
        ciutat: {
            NOM: '',
            DESCRIPCIO: '',
            IMATGE: '',
            POBLACIO: '',
            PROVINCIA_ID: ''
        },

        carregarCiutat() {
            fetch('/api/ciutats/' + id)
                .then(function (resposta) {
                    return resposta.json();
                })
                .then((dades) => {
                    this.ciutat = dades;
                });
        },

        modificarCiutat() {
            fetch('/api/ciutats/' + id, {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(this.ciutat)
            })
                .then(function (resposta) {
                    return resposta.json();
                })
                .then(function () {
                    mostrarCiutats();
                });
        }
    };
};
```

---

## `public/js/spa.js`

```javascript
function mostrarModificarCiutat(id) {
    document.getElementById("spa").innerHTML = vistaModificarCiutat(id);
    Alpine.initTree(document.getElementById("spa"));
}
```





# Provincies

## `routes/provincies.js`

```javascript
router.put('/:id', function(req, res) {
    db('PROVINCIA')
        .where('ID', req.params.id)
        .update({
            NOM: req.body.NOM,
            PAIS_ID: req.body.PAIS_ID
        })
        .then(function(resultat) {
            res.json({ missatge: 'Província modificada correctament' });
        })
        .catch(function(error) {
            res.status(500).json({ error: 'Error en la base de dades' });
        });
});
```

---

## `public/js/provincies-vista.js`

### Botó `Modificar`

```html
<button type="button"
        class="success"
        @click="mostrarModificarProvincia(provincia.ID)">
    Modificar
</button>
```

### Vista de modificació

```javascript
function vistaModificarProvincia(id) {
    return `
        <section x-data="modificarProvinciaApp(${id})"
                 x-init="carregarProvincia()">

            <h2>Modificar província</h2>

            <form @submit.prevent="modificarProvincia()">

                <label for="nom">Nom</label>
                <input id="nom" type="text" x-model="provincia.NOM">

                <label for="pais">País</label>
                <input id="pais" type="number" x-model="provincia.PAIS_ID">

                <p>
                    <button type="submit">Guardar canvis</button>

                    <button type="button"
                            class="warning"
                            onclick="mostrarProvincies()">
                        Cancel·lar
                    </button>
                </p>

            </form>

        </section>
    `;
}
```

### App Alpine

```javascript
window.modificarProvinciaApp = function (id) {
    return {
        provincia: {
            NOM: '',
            PAIS_ID: ''
        },

        carregarProvincia() {
            fetch('/api/provincies/' + id)
                .then(function (resposta) {
                    return resposta.json();
                })
                .then((dades) => {
                    this.provincia = dades;
                });
        },

        modificarProvincia() {
            fetch('/api/provincies/' + id, {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(this.provincia)
            })
                .then(function (resposta) {
                    return resposta.json();
                })
                .then(function () {
                    mostrarProvincies();
                });
        }
    };
};
```

---

## `public/js/spa.js`

```javascript
function mostrarModificarProvincia(id) {
    document.getElementById("spa").innerHTML = vistaModificarProvincia(id);
    Alpine.initTree(document.getElementById("spa"));
}
```

---


# Paisos


## `routes/paisos.js`

```javascript
router.put('/:id', function(req, res) {
    db('PAIS')
        .where('ID', req.params.id)
        .update({
            NOM: req.body.NOM
        })
        .then(function(resultat) {
            res.json({ missatge: 'País modificat correctament' });
        })
        .catch(function(error) {
            res.status(500).json({ error: 'Error en la base de dades' });
        });
});
```

---

## `public/js/paisos-vista.js`

### Botó `Modificar`

```html
<button type="button"
        class="success"
        @click="mostrarModificarPais(pais.ID)">
    Modificar
</button>
```

### Vista de modificació

```javascript
function vistaModificarPais(id) {
    return `
        <section x-data="modificarPaisApp(${id})"
                 x-init="carregarPais()">

            <h2>Modificar país</h2>

            <form @submit.prevent="modificarPais()">

                <label for="nom">Nom</label>
                <input id="nom" type="text" x-model="pais.NOM">

                <p>
                    <button type="submit">Guardar canvis</button>

                    <button type="button"
                            class="warning"
                            onclick="mostrarPaisos()">
                        Cancel·lar
                    </button>
                </p>

            </form>

        </section>
    `;
}
```

### App Alpine

```javascript
window.modificarPaisApp = function (id) {
    return {
        pais: {
            NOM: ''
        },

        carregarPais() {
            fetch('/api/paisos/' + id)
                .then(function (resposta) {
                    return resposta.json();
                })
                .then((dades) => {
                    this.pais = dades;
                });
        },

        modificarPais() {
            fetch('/api/paisos/' + id, {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(this.pais)
            })
                .then(function (resposta) {
                    return resposta.json();
                })
                .then(function () {
                    mostrarPaisos();
                });
        }
    };
};
```

---

## `public/js/spa.js`

```javascript
function mostrarModificarPais(id) {
    document.getElementById("spa").innerHTML = vistaModificarPais(id);
    Alpine.initTree(document.getElementById("spa"));
}
```

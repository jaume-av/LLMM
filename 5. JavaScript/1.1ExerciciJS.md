---

title: 1.1- Exercicis JavaScript Consola
layout: default
parent: 1.- JavaScript
nav_order: 10
has_children: true

---

# Exercicis JavaScript

## 1. Introducció a la consola

### 1. Obrir la consola

1. Obri qualsevol pàgina web (per exemple, google.com).
2. Prem **F12**.
3. Ves a la pestanya **Consola**.
4. Escriu:

```javascript
2 + 2
```

Prem **Enter**.

La consola mostrarà:

```
4
```

La consola és un lloc on podem escriure JavaScript i veure el resultat immediatament.

Tot el que escrius allí s’executa en el moment.

---

## Exercici guiat 1 – Expressions i variables

### Pas 1

Escriu en la consola:

```javascript
const preu = 50;
```

Prem Enter.

No apareix res perquè només hem declarat una variable.

Ara escriu:

```javascript
preu
```

Prem Enter.

La consola mostrarà:

```
50
```

La consola sempre mostra el valor de l’última expressió.

---

### Pas 2

Ara escriu:

```javascript
const descompte = 10;
```

Després:

```javascript
preu - descompte
```

La consola mostrarà:

```
40
```

Això és una expressió matemàtica.

---

### Pas 3

Ara guarda el resultat en una variable:

```javascript
const preuFinal = preu - descompte;
```

Comprova el valor:

```javascript
preuFinal
```

---

### Pas 4 – Condició

Ara escriu:

```javascript
preuFinal > 30
```

La consola mostrarà:

```
true
```

Això és una comparació.

Ara escriu:

```javascript
if (preuFinal > 30) {
    "Preu alt"
} else {
    "Preu baix"
}
```

La consola mostrarà:

```
"Preu alt"
```

En consola, si l’última línia és una cadena, la mostra automàticament.

---

## Exercici guiat 2 – Entrada i condició


### Pas 1 – Demanar una edat

Escriu:

```javascript
const edat = Number(prompt("Introdueix la teua edat:"));
```

S’obrirà una finestra.

Escriu un número i accepta.

Ara escriu:

```javascript
edat
```

Per comprovar el valor.

---

### Pas 2 – Fer una condició

Ara escriu:

```javascript
if (edat >= 18) {
    "Major d'edat"
} else {
    "Menor d'edat"
}
```

La consola mostrarà el resultat segons el número introduït.

---

### Pas 3 – Afegir una expressió

Escriu:

```javascript
edat + 5
```

I observa el resultat.

Estem combinant:

* Entrada
* Conversió
* Expressió matemàtica
* Condició


---

## Exercici 3 – Control d’accés

Fes el següent en la consola:

1. Demana a l’usuari la seua edat.
2. Demana si té carnet (pots usar `confirm()`).
3. Mostra:

   * "Pot conduir" si té 18 o més i té carnet.
   * "No pot conduir" en qualsevol altre cas.

Condicions:

* Has d’utilitzar `Number(prompt(...))`.
* Has d’utilitzar `===`.
* Has d’utilitzar `&&`.

No copies codi anterior. Escriu-lo des de zero.

---

>Important 
>Quan treballen en consola:
>
>* Si volen reiniciar variables, poden escriure:
>
>```javascript
>clear()
>```
>
>* O recarregar la pàgina (F5).
>
>Cada recàrrega elimina les variables anteriors.

---




